# Visual Journal Bot

Streamlit app that turns natural language journal entries into animated visual scenes on a Paper.js canvas.

## Setup

```bash
pip install -r requirements.txt
export GEMINI_API_KEY="your_api_key_here"
streamlit run app.py
